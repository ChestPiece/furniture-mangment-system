'use server'

export async function getLowStockItems({ limit = 5 }: { limit?: number } = {}) {
  // TODO: Implement Convex Query
  console.log('TODO: Get low stock items via Convex', limit)
  return []
}
